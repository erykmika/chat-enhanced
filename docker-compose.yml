# Docker compose config for local development

services:
  db:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-devdb}
      POSTGRES_USER: ${POSTGRES_USER:-devuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpass}
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-devuser} -d ${POSTGRES_DB:-devdb}"]
      interval: 10s
      timeout: 5s
      retries: 5

  mail:
    image: mailhog/mailhog
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8025/ >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: backend/webapp/Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      mail:
        condition: service_healthy
    ports:
      - "8000:8000"
    environment:
      JWT_SECRET: "dev-jwt-secret-change-me"

      # Database (use compose service name, not localhost)
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ${POSTGRES_DB:-devdb}
      POSTGRES_USER: ${POSTGRES_USER:-devuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpass}

      MAIL_SERVER: mail
      MAIL_PORT: "1025"
      MAIL_DEFAULT_SENDER: "chat@mail.int"
      MAIL_USERNAME: ""
      MAIL_PASSWORD: ""

      CORS_ALLOWED_ORIGINS: "*"

      # Used to generate the confirmation link sent by email.
      FRONTEND_ROOT_DOMAIN: "http://localhost:3000"

    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  ws_server:
    build:
      context: .
      dockerfile: backend/ws_server/Dockerfile
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_started
      redis:
        condition: service_healthy
    entrypoint: []
    command: ["python", "-m", "backend.ws_server.server"]
    environment:
      JWT_SECRET: "dev-jwt-secret-change-me"
      WS_HOST: "0.0.0.0"
      WS_PORT: "8001"
      REDIS_URL: "redis://redis:6379/0"
      REDIS_CONNECT_RETRIES: "10"
      REDIS_CONNECT_DELAY: "1"
      RUN_MIGRATIONS: "0"

  ws_lb:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      - ws_server
    ports:
      - "8011:8011"
    volumes:
      - ./backend/ws_server/nginx.conf:/etc/nginx/nginx.conf:ro

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      # Used at build time by Vite; pointing to backend service name works within the compose network.
      - VITE_API_BASE_URL=http://localhost:8000
      - VITE_WS_BASE_URL=ws://localhost:8011
    depends_on:
      - backend

volumes:
  db_data:
    driver: local
