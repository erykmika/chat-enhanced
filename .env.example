# Compose-level environment (used by docker compose automatically when placed at repo root as .env)
# Copy to .env for local development.

POSTGRES_DB=devdb
POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpass
